export class Credentials {
  a: string;
  accessToken: string;
  idToken: string;
  providerId: string;
  signInMethod: string;
}

export class FirebaseCredentials {
  oauthAccessToken: string;
  oauthIdToken: string;
  providerId: string;
  signInMethod: string;
}
